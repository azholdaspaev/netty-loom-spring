spring.application.name=tomcat

# Fixed credentials for the secured benchmark scenario, matching the Netty example so both targets
# authenticate identically. Without them Boot generates a random password at startup, which the k6
# script cannot know. Profile-independent, so they live here rather than in the profile files.
spring.security.user.name=bench
spring.security.user.password=bench
