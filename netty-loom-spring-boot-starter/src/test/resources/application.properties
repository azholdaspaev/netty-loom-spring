# Spring Security is a test dependency for the error-page tests alone (issue #38), but its
# auto-configuration would otherwise protect every other test application in this module. Off by
# default; a higher-precedence property source replaces this key rather than adding to it, so a test
# that wants Security sets spring.autoconfigure.exclude back to empty, and a test that wants an
# exclusion of its own has to repeat these four entries alongside it.
spring.autoconfigure.exclude=\
  org.springframework.boot.security.autoconfigure.SecurityAutoConfiguration,\
  org.springframework.boot.security.autoconfigure.UserDetailsServiceAutoConfiguration,\
  org.springframework.boot.security.autoconfigure.web.servlet.SecurityFilterAutoConfiguration,\
  org.springframework.boot.security.autoconfigure.web.servlet.ServletWebSecurityAutoConfiguration
