{
  "groups": [
    {
      "name": "server.netty",
      "description": "Configuration properties for the Netty-based web server that replaces Tomcat in this starter."
    }
  ],
  "properties": [
    {
      "name": "server.netty.boss-threads",
      "description": "Number of threads in the boss event loop group that accepts incoming connections. One is typically sufficient."
    },
    {
      "name": "server.netty.worker-threads",
      "description": "Number of threads in the worker event loop group that processes I/O for accepted connections. Use 0 to apply Netty's default of two times the number of available processors."
    },
    {
      "name": "server.netty.tcp-keep-alive",
      "description": "Enables TCP SO_KEEPALIVE on accepted child channels. This is the socket-level keep-alive, independent of HTTP keep-alive semantics. HTTP keep-alive is protocol behaviour and is always on."
    },
    {
      "name": "server.netty.shutdown-grace-period",
      "description": "Maximum time to wait for in-flight HTTP requests to complete when Spring Boot invokes graceful shutdown (server.shutdown=graceful). After this deadline, remaining connections are force-closed. Defaults to 30 seconds, matching spring.lifecycle.timeout-per-shutdown-phase."
    },
    {
      "name": "server.netty.read-timeout",
      "description": "How long the server waits on the client before closing the connection: one interval to deliver a complete request, and one interval of sitting idle between requests on a keep-alive connection. Time spent handling a request does not count against it, so a handler slower than the timeout still gets to answer. A connection that expires is closed without a response. Use 0 to disable. Defaults to 30 seconds."
    },
    {
      "name": "server.netty.transport",
      "type": "java.lang.String",
      "description": "Netty I/O transport to use. 'auto' picks the best native transport for the platform (epoll on Linux, kqueue on macOS/BSD) and falls back to NIO when none is available. 'nio' forces the portable Java NIO transport. 'epoll' or 'kqueue' force that native transport and fail fast at startup if it is not available on the platform.",
      "defaultValue": "auto"
    }
  ],
  "hints": [
    {
      "name": "server.netty.transport",
      "values": [
        {
          "value": "auto",
          "description": "Auto-select the best native transport (epoll/kqueue), falling back to NIO."
        },
        {
          "value": "nio",
          "description": "Portable Java NIO transport. Works everywhere."
        },
        {
          "value": "epoll",
          "description": "Linux native transport. Fails fast if unavailable."
        },
        {
          "value": "kqueue",
          "description": "macOS/BSD native transport. Fails fast if unavailable."
        }
      ]
    },
    {
      "name": "server.netty.worker-threads",
      "values": [
        {
          "value": 0,
          "description": "Use Netty's default (2 x Runtime.getRuntime().availableProcessors())."
        }
      ]
    }
  ]
}
