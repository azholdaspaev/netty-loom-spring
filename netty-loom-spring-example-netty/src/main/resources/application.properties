spring.application.name=netty-loom
server.port=18080

# Fixed credentials for the secured benchmark scenario. Without them Boot generates a random
# password at startup, which the k6 script cannot know. Not a secret — this app serves
# Thread.sleep(50) on localhost.
spring.security.user.name=bench
spring.security.user.password=bench
